# Οδηγίες Εγκατάστασης & Εκτέλεσης — UniBite

## Τι χρειάζεσαι

| Λογισμικό | Έκδοση | Σκοπός |
|---|---|---|
| Windows | 10 / 11 | Λειτουργικό σύστημα |
| XAMPP | 8.x | Apache (web server) + MariaDB (βάση) + PHP |
| Browser | Chrome / Firefox / Edge | Πρόσβαση στην εφαρμογή |

> Δεν χρειάζεται Node.js, Python, ή οποιοδήποτε άλλο runtime. Μόνο XAMPP.

---

## Βήμα 1 — Εγκατάσταση XAMPP

1. Κατέβασε το XAMPP από: https://www.apachefriends.org
2. Εκτέλεσε τον installer. Επέλεξε οπωσδήποτε: **Apache**, **MySQL**, **PHP**
3. Εγκατάστησε στον προεπιλεγμένο φάκελο: `C:\xampp`

---

## Βήμα 2 — Έναρξη XAMPP

1. Άνοιξε το **XAMPP Control Panel** (από το Start Menu ή `C:\xampp\xampp-control.exe`)
2. Πάτα **Start** δίπλα στο **Apache**
3. Πάτα **Start** δίπλα στο **MySQL**
4. Και τα δύο πρέπει να γίνουν πράσινα

### Αν το MySQL δεν ξεκινά (port conflict)

Αν έχεις εγκατεστημένο MySQL 8.x ξεχωριστά, το XAMPP MariaDB θα βρει κατειλημμένη την πόρτα 3307. Λύση:

1. XAMPP Control Panel → MySQL → **Config** → `my.ini`
2. Βρες τη γραμμή `port=3307` και άλλαξέ την σε `port=3307`
3. Κάνε **Stop** και ξανά **Start** το MySQL

---

## Βήμα 3 — Τοποθέτηση αρχείων

Αντέγραψε τον φάκελο `Unibite` μέσα στο:

```
C:\xampp\htdocs\Unibite\
```

Τελική δομή:

```
C:\xampp\htdocs\Unibite\
├── frontend\
│   ├── index.html
│   ├── style.css
│   └── script.js
├── backend\
│   ├── config.php
│   ├── auth.php
│   ├── ads.php
│   ├── requests.php
│   ├── stats.php
│   └── unibite.sql
├── uploads\              ← δημιουργείται αυτόματα από το PHP
│   └── avatars\          ← φωτογραφίες προφίλ χρηστών
└── refresh.bat
```

> Ο φάκελος `uploads/` δημιουργείται αυτόματα την πρώτη φορά που κάποιος ανεβάζει εικόνα. Δεν χρειάζεται χειροκίνητη δημιουργία.

---

## Βήμα 4 — Ρύθμιση config.php

Άνοιξε `C:\xampp\htdocs\Unibite\backend\config.php` και έλεγξε:

```php
$host     = 'localhost';
$port     = 3307;
$dbname   = 'unibite_db';
$username = 'root';
$password = '';     // κενό — XAMPP δεν έχει password για root by default
```

---

## Βήμα 5 — Εισαγωγή βάσης δεδομένων

Άνοιξε **Command Prompt** (cmd) και τρέξε:

```
chcp 65001
C:\xampp\mysql\bin\mysql.exe -u root -P 3307 -h 127.0.0.1 < "C:\xampp\htdocs\Unibite\backend\unibite.sql"
```

> Αν το XAMPP MySQL τρέχει σε διαφορετική πόρτα, άλλαξε αντίστοιχα το `-P 3307` και το `config.php`

Αν δεν εμφανιστεί κανένα μήνυμα error, η εισαγωγή πέτυχε.

### Επαλήθευση

```
C:\xampp\mysql\bin\mysql.exe -u root -P 3307 -h 127.0.0.1 -e "SHOW DATABASES;"
```

Πρέπει να δεις `unibite_db` στη λίστα.

---

## Βήμα 6 — Εκκίνηση εφαρμογής

Άνοιξε browser και πήγαινε:

```
http://localhost/Unibite/frontend/
```

> **Σημαντικό:** Μην ανοίξεις το `index.html` με double-click. Χρειάζεται Apache για να λειτουργήσει το PHP backend.

---

## Λογαριασμοί για δοκιμή

Σύνδεση μόνο με email — χωρίς password.

| Email | Ρόλος |
|---|---|
| `marios@uni.gr` | Μάγειρας |
| `elena@uni.gr` | Μάγειρας |
| `giorgos@uni.gr` | Μάγειρας |
| `anna@uni.gr` | Μάγειρας |
| `katerina@uni.gr` | Καταναλωτής |
| `nikos@uni.gr` | Καταναλωτής |

---

## Reset βάσης δεδομένων

Για να επαναφέρεις τα δεδομένα στην αρχική κατάσταση, τρέξε:

```
C:\xampp\htdocs\Unibite\refresh.bat
```

Ή χειροκίνητα:

```
C:\xampp\mysql\bin\mysql.exe -u root -P 3307 -h 127.0.0.1 < "C:\xampp\htdocs\Unibite\backend\unibite.sql"
```

Το αρχείο SQL ξεκινάει με `DROP DATABASE IF EXISTS unibite_db` οπότε διαγράφει και ξαναδημιουργεί τα πάντα.

---

## Αντιμετώπιση προβλημάτων

### "Σφάλμα σύνδεσης με τον server"

- Έλεγξε ότι Apache **και** MySQL τρέχουν στο XAMPP Control Panel (πράσινα)
- Βεβαιώσου ότι ανοίγεις `http://localhost/...` και όχι `file://...`

### "Plugin caching_sha2_password could not be loaded"

Το XAMPP MariaDB client προσπαθεί να συνδεθεί στο MySQL 8.x αντί για το MariaDB. Λύση: πρόσθεσε `-h 127.0.0.1` στην εντολή mysql.

### ERROR 1050: Table already exists

Η βάση υπάρχει ήδη. Τρέξε πρώτα:

```
C:\xampp\mysql\bin\mysql.exe -u root -P 3307 -h 127.0.0.1 -e "DROP DATABASE IF EXISTS unibite_db;"
```

### Λευκή σελίδα / PHP error

Άνοιξε `http://localhost/Unibite/backend/auth.php?action=me` στον browser. Αν δεις JSON response (έστω και error), το PHP λειτουργεί. Αν δεις κενό ή HTML error, έλεγξε το `config.php`.

---

## Χρήση PhpMyAdmin (προαιρετικά)

Για οπτική διαχείριση της βάσης, άνοιξε: `http://localhost/phpmyadmin`

Server: `127.0.0.1`, Port: `3307` (ή `3307`), Username: `root`, Password: (κενό)
